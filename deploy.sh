#!/bin/bash
git config --global user.name "Kalindi Marketing Agent"
git config --global user.email "bot@kalindimarketing.com"
git add .
git commit -m "Initial commit - Kalindi Marketing Website v1"
git branch -M main
